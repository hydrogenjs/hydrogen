{
  "name": "webapp",
  "version": "1.1.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.0",
    "html-template-tag": "^3.1.0",
    "hydrogen-cli": "^0.6.2"
  },
  "scripts": {
    "reload": "npx cross-env npx nodemon -w ./layouts -w ./pages -w ./public --exec \"npx hydrogen build --dev\"",
    "serve": "npx cross-env npx live-server ./dist",
    "dev": "npx npm-run-all --parallel reload serve",
    "build": "npx hydrogen build",
    "test:cli": "../bin/run build --dev" 
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
